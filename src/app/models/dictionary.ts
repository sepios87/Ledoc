export enum ElementsDictionary {
    drugs = "drugs",
    repeats = "repeats",
    periods = "periods",
    bloodgroups = "bloodgroups",
    gender = "gender"
}

export interface Dictionary {
    id: number,
    label: string
}
